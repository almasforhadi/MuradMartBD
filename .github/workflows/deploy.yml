name: Django CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Docker Image
      run: docker build -t eshop .

    - name: Run Tests
      run: echo "No tests yet"

    - name: Success
      run: echo "Build Successful 🚀"